# TikTok Shop Screener Ingestion

Ingestion adapters are present but return no seeded records in the blank shell.

Future TikTok Shop data should populate the existing normalized signal and graph structures without changing routes, node models, relationship helpers, or UI components.
